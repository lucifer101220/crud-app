import * as React from 'react';
import { IndexedObject } from '../utils/type';

const Home: React.FC<IndexedObject> = () => {
  return (
    <main>
      <h1>Home page</h1>
    </main>
  );
};

export default Home;
